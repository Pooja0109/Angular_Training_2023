import { PipeRangePipe } from './pipe-range.pipe';

describe('PipeRangePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeRangePipe();
    expect(pipe).toBeTruthy();
  });
});
