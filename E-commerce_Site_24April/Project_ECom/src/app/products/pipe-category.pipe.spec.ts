import { PipeCategoryPipe } from './pipe-category.pipe';

describe('PipeCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
